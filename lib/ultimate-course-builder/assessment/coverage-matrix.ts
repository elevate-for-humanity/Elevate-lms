export type CoverageState={objectiveId:string;competencyIds:string[];taughtEvidenceIds:string[];demonstrationEvidenceIds:string[];practiceEvidenceIds:string[];assessmentIds:string[];mastered:boolean;practicalEvidenceApproved:boolean;requiresPracticalEvidence:boolean};
export function buildAssessmentCoverageMatrix(rows:CoverageState[]){const entries=rows.map(r=>({...r,TAUGHT:r.taughtEvidenceIds.length>0,DEMONSTRATED:r.demonstrationEvidenceIds.length>0,PRACTICED:r.practiceEvidenceIds.length>0,ASSESSED:r.assessmentIds.length>0,MASTERED:r.mastered&&(!r.requiresPracticalEvidence||r.practicalEvidenceApproved)}));const blockers:string[]=[];for(const e of entries){if(e.ASSESSED&&!e.TAUGHT)blockers.push(`UNTaught_ASSESSMENT:${e.objectiveId}`);if(!e.ASSESSED)blockers.push(`OBJECTIVE_UNASSESSED:${e.objectiveId}`);if(e.requiresPracticalEvidence&&!e.practicalEvidenceApproved)blockers.push(`PRACTICAL_EVIDENCE_MISSING:${e.objectiveId}`);}return {entries,blockers,releaseEligible:blockers.length===0};}
