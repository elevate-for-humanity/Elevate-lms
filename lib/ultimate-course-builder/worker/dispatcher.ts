import type {SupabaseClient} from '@supabase/supabase-js';
export async function dispatchUltimateJob(db:SupabaseClient,job:{id:string;build_id:string},workerId:string){const endpoint=process.env.ULTIMATE_EXECUTOR_URL;const token=process.env.ULTIMATE_EXECUTOR_TOKEN;if(!endpoint||!token)throw new Error('ULTIMATE_EXECUTOR_CONFIG_REQUIRED');const response=await fetch(endpoint,{method:'POST',headers:{'content-type':'application/json','authorization':`Bearer ${token}`},body:JSON.stringify({jobId:job.id,buildId:job.build_id,workerId})});if(!response.ok)throw new Error(`ULTIMATE_EXECUTOR_FAILED:${response.status}:${(await response.text()).slice(0,500)}`);return response.json();}
