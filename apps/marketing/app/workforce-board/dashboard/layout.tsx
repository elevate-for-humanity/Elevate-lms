export default function WorkforceBoardDashboardLayout({ children }: { children: React.ReactNode }) {
  // The parent /workforce-board layout owns authorization and the canonical shell.
  return children;
}
