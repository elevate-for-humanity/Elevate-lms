export default function CaseManagerDashboardLayout({ children }: { children: React.ReactNode }) {
  // The parent /case-manager layout owns authorization and the canonical shell.
  return children;
}
