import {NextRequest,NextResponse} from 'next/server';import {apiRequireAdmin} from '@/lib/admin/guards';import {requireAdminClient} from '@/lib/supabase/admin';import {UltimateJobQueue} from '@/lib/ultimate-course-builder/worker/job-queue';
export const runtime='nodejs';export const dynamic='force-dynamic';
export async function POST(req:NextRequest){const auth=await apiRequireAdmin(req);if(auth.error)return auth.error;const body=await req.json().catch(()=>null);if(!body?.buildId)return NextResponse.json({error:'buildId is required'},{status:400});const db=await requireAdminClient();const {data:build,error}=await db.from('ultimate_course_builds').select('id,status').eq('id',body.buildId).single();if(error||!build)return NextResponse.json({error:'Ultimate build not found'},{status:404});const job=await new UltimateJobQueue(db as any).enqueue(build.id,{requestedBy:auth.user?.id??null});return NextResponse.json({ok:true,queued:true,buildId:build.id,jobId:job?.id??null},{status:202});}
