import {deriveUniversalInstructionalDepth} from '../instructional/universal-depth-contract';import {UltimateBuildRunner,type UltimateRunContext} from './build-runner';import type {UltimateCredentialProfile,UltimateBuildStep} from './types';import type {UltimatePersistencePort} from './ports';import type {UltimateArtifactPort} from '../artifacts/types';
export type UltimateCoursePlan={buildId:string;courseId:string;profile:UltimateCredentialProfile};
export async function runUltimateCourse(plan:UltimateCoursePlan,makeRunner:(competencyId:string)=>UltimateBuildRunner,persistence:UltimatePersistencePort,artifactStore?:UltimateArtifactPort){await persistence.updateBuild({buildId:plan.buildId,status:'running',currentStep:'standards_lock'});const lessons=[];for(const competency of plan.profile.competencies){const lesson=await persistence.createLesson({buildId:plan.buildId,lessonKey:competency.id,competencyId:competency.id});const checkpoint=await persistence.loadLessonCheckpoint({lessonBuildId:lesson.id});const context:UltimateRunContext={buildId:plan.buildId+':'+competency.id,courseId:plan.courseId,profile:plan.profile,lessonBuildId:lesson.id,artifacts:{...(checkpoint.artifacts as any),instructional_depth:deriveUniversalInstructionalDepth(competency) as any},findings:checkpoint.findings,passedSteps:new Set(checkpoint.passedSteps as UltimateBuildStep[]),persistStep:async input=>{await persistence.updateBuild({buildId:plan.buildId,status:'running',currentStep:input.step});await persistence.recordStep({lessonBuildId:lesson.id,...input});},persistFinding:async finding=>persistence.recordFinding({buildId:plan.buildId,finding}),persistArtifact:artifactStore?async({step,artifacts})=>{await artifactStore.createVersion({buildId:plan.buildId,lessonBuildId:lesson.id,stage:step,artifactType:'stage_output',logicalKey:`lesson:${lesson.id}:stage:${step}`,content:artifacts,provenance:{courseId:plan.courseId,competencyId:competency.id},generatedBy:'ultimate-worker'});}:undefined};const result=await makeRunner(competency.id).run(context);await persistence.finishLesson({lessonBuildId:lesson.id,status:result.findings.some(f=>f.severity==='error')?'built_with_findings':'built',artifacts:result.artifacts,findings:result.findings});lessons.push({competencyId:competency.id,lessonBuildId:lesson.id,result});}const findings=lessons.flatMap(x=>x.result.findings);await persistence.updateBuild({buildId:plan.buildId,status:'built',currentStep:'credential_release' as UltimateBuildStep,findings});return {buildId:plan.buildId,courseId:plan.courseId,lessons,findings,completed:lessons.length===plan.profile.competencies.length};}
