import type {SupabaseClient} from '@supabase/supabase-js';import {UltimateJobQueue} from './job-queue';import {createUltimateRuntime} from '../core/runtime-factory';import {createProductionHandlers} from '../core/production-handlers';import {UltimateBuildRunner} from '../core/build-runner';import {runUltimateCourse} from '../core/full-course-runner';import type {UltimateCredentialProfile} from '../core/types';
export async function processUltimateJob(db:SupabaseClient,workerId:string){const queue=new UltimateJobQueue(db);const job=await queue.claim(workerId,300);if(!job)return {claimed:false};let timer:ReturnType<typeof setInterval>|null=setInterval(()=>void queue.heartbeat(job.id,workerId,300).catch(()=>{}),60000);try{const {data:build,error}=await db.from('ultimate_course_builds').select('id,course_id,profile,status').eq('id',job.build_id).single();if(error||!build)throw error??new Error('ULTIMATE_BUILD_NOT_FOUND');const profile=build.profile as UltimateCredentialProfile;if(!profile?.competencies?.length)throw new Error('ULTIMATE_PROFILE_HAS_NO_COMPETENCIES');const runtime=await createUltimateRuntime(db);const handlers=createProductionHandlers(runtime);const result=await runUltimateCourse({buildId:build.id,courseId:build.course_id,profile},()=>new UltimateBuildRunner(handlers as any),runtime.persistence,runtime.artifacts);await queue.complete(job.id,workerId);return {claimed:true,completed:true,jobId:job.id,result};}catch(error){const message=error instanceof Error?error.message:String(error);await queue.fail(job.id,workerId,message,true);return {claimed:true,completed:false,jobId:job.id,error:message};}finally{if(timer)clearInterval(timer);timer=null;}}
