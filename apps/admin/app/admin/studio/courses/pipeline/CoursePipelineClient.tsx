'use client';

// Canonicalized: Admin Studio uses the single shared Course Builder pipeline client.
// The shared client owns the production API contract at /api/admin/course-builder/pipeline.
export { default } from '@/components/admin/course-builder/CoursePipelineClient';
