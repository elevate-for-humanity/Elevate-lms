// ARCHIVED — FSSA SNAP E&T program ended.
export const metadata = { robots: { index: false } };

import { redirect } from 'next/navigation';
export default function FssaArchivedPage() { redirect('/dashboard'); }
