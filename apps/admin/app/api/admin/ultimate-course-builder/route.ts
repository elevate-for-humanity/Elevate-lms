import {NextRequest,NextResponse} from 'next/server';import {apiRequireAdmin} from '@/lib/admin/guards';import {requireAdminClient} from '@/lib/supabase/admin';
export const runtime='nodejs';export const dynamic='force-dynamic';
export async function GET(req:NextRequest){const auth=await apiRequireAdmin(req);if(auth.error)return auth.error;const db=await requireAdminClient();const id=req.nextUrl.searchParams.get('buildId');if(id){const {data,error}=await db.from('ultimate_course_builds').select('*,ultimate_lesson_builds(*,ultimate_lesson_steps(*))').eq('id',id).single();if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({build:data});}const {data,error}=await db.from('ultimate_course_builds').select('*').order('created_at',{ascending:false}).limit(50);return error?NextResponse.json({error:error.message},{status:500}):NextResponse.json({builds:data??[]});}
export async function POST(req:NextRequest){const auth=await apiRequireAdmin(req);if(auth.error)return auth.error;const body=await req.json();const db=await requireAdminClient();if(body.action==='create'){const {data,error}=await db.from('ultimate_course_builds').insert({course_id:body.courseId,profile:body.profile,status:'initializing',current_step:'standards_lock'}).select('*').single();return error?NextResponse.json({error:error.message},{status:500}):NextResponse.json({ok:true,build:data});}if(body.action==='status'){const {data,error}=await db.from('ultimate_course_builds').select('*').eq('id',body.buildId).single();return error?NextResponse.json({error:error.message},{status:500}):NextResponse.json({ok:true,build:data});}return NextResponse.json({error:'Unsupported Ultimate action'},{status:400});}
