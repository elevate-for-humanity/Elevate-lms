import {getRegisteredProgramStandard,isRegisteredProgramSlug} from '@/lib/apprenticeship/registered-program-contract';import type {UltimateStandardsSource} from './standards-source';import {universalCompetency} from './standards-source';import type {UltimateCredentialProfile} from '../core/types';
export class UltimateAppendixAStandardsSource implements UltimateStandardsSource{supports(input:{programSlug?:string}){return isRegisteredProgramSlug(input.programSlug);}async load(input:{programSlug?:string}):Promise<UltimateCredentialProfile>{if(!input.programSlug)throw new Error('ULTIMATE_REGISTERED_PROGRAM_SLUG_REQUIRED');const contract=getRegisteredProgramStandard(input.programSlug);if(!contract)throw new Error('ULTIMATE_REGISTERED_STANDARD_NOT_FOUND');const s=contract.standard;return {id:`registered:${contract.standardKey}:${s.rapidsCode}`,title:`${s.occupationTitle} Registered Apprenticeship`,authority:`U.S. Department of Labor Office of Apprenticeship / ${contract.sponsor.sponsor}`,jurisdiction:'US/Indiana',standardVersion:contract.sponsor.revisionDate,effectiveDate:contract.sponsor.revisionDate,sourceDocuments:['lib/apprenticeship/registered-program-contract.ts'],socCodes:[s.onetSocCode],trainingRequirements:{instructionalHours:contract.completion.requiredRtiHours,ojlHours:s.totalOjlHours},competencies:s.competencies.map(c=>universalCompetency({id:c.id,title:c.category,description:c.description,requirementIds:[`RAPIDS:${s.rapidsCode}:APPENDIX_A:${c.sourceLabel??c.id}`],practical:true,safety:/clean|steril|sanitize|protective|safety|infection/i.test(c.category+' '+c.description)})),examBlueprint:{domains:s.relatedInstruction.map(x=>({id:x.title.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,''),title:x.title,weight:s.relatedInstructionHours?x.hours/s.relatedInstructionHours:undefined}))}};}}
