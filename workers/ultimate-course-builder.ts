import {createClient} from '@supabase/supabase-js';import {UltimateJobQueue} from '../lib/ultimate-course-builder/worker/job-queue';import {dispatchUltimateJob} from '../lib/ultimate-course-builder/worker/dispatcher';
const url=process.env.NEXT_PUBLIC_SUPABASE_URL??process.env.SUPABASE_URL;const key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)throw new Error('ULTIMATE_WORKER_SUPABASE_CONFIG_REQUIRED');const db=createClient(url,key,{auth:{persistSession:false,autoRefreshToken:false}});const queue=new UltimateJobQueue(db);const workerId=process.env.ULTIMATE_WORKER_ID??'ultimate-'+process.pid;const pollMs=Math.max(1000,Number(process.env.ULTIMATE_WORKER_POLL_MS??5000));let stopping=false;for(const signal of ['SIGTERM','SIGINT'] as const)process.on(signal,()=>{stopping=true;});async function main(){while(!stopping){const job=await queue.claim(workerId,300);if(!job){await new Promise(r=>setTimeout(r,pollMs));continue;}try{await dispatchUltimateJob(db,job,workerId);}catch(error){await queue.fail(job.id,workerId,error instanceof Error?error.message:String(error),true);}}}main().catch(error=>{console.error('[UltimateWorker] fatal',error);process.exit(1);});
