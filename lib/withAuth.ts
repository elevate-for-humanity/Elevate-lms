/**
 * @deprecated Compatibility export.
 *
 * The canonical authentication wrapper is lib/with-auth.ts. Keep this file only
 * so historical imports do not recreate a second RBAC implementation.
 */
export { withAuth } from './with-auth';
