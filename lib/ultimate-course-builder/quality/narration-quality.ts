export type Metric<T>={value:T|null;measured:boolean;source?:string};
export interface NarrationMetrics{wordsPerMinute:Metric<number>;roboticScore:Metric<number>;monotoneScore:Metric<number>;clippedWords:Metric<number>;unnaturalPauseCount:Metric<number>;pronunciationFailures:Metric<string[]>;repeatedAudioRatio:Metric<number>;durationSeconds:Metric<number>}
export function evaluateNarration(m:NarrationMetrics){const failures:string[]=[];const unknown:string[]=[];const need=<T>(name:string,x:Metric<T>)=>{if(!x.measured||x.value===null)unknown.push(name);};need('wordsPerMinute',m.wordsPerMinute);need('roboticScore',m.roboticScore);need('monotoneScore',m.monotoneScore);need('clippedWords',m.clippedWords);need('unnaturalPauseCount',m.unnaturalPauseCount);need('pronunciationFailures',m.pronunciationFailures);need('repeatedAudioRatio',m.repeatedAudioRatio);need('durationSeconds',m.durationSeconds);if(m.wordsPerMinute.value!==null&&(m.wordsPerMinute.value<115||m.wordsPerMinute.value>150))failures.push('PACE_OUT_OF_RANGE');if((m.roboticScore.value??0)>.2)failures.push('ROBOTIC_CADENCE');if((m.monotoneScore.value??0)>.3)failures.push('MONOTONE');if((m.clippedWords.value??0)>0)failures.push('CLIPPED_WORDS');if((m.unnaturalPauseCount.value??0)>2)failures.push('UNNATURAL_PAUSES');if((m.pronunciationFailures.value??[]).length)failures.push('PRONUNCIATION_FAILURE');if((m.repeatedAudioRatio.value??0)>.05)failures.push('REPEATED_AUDIO');if(unknown.length)failures.push('NARRATION_MEASUREMENT_INCOMPLETE');return {pass:failures.length===0,failures,unknown};}
